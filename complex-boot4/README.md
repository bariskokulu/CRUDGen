# complex-boot4

Same sources as **complex-boot3** against Spring Boot **4** / Jackson 3: **`io.github.vishwakarma:zjsonpatch`**, MapStruct **1.6.x**. Jackson / validation types come from the Boot 4 compile classpath.

```bash
./gradlew :complex-boot4:compileJava
```
